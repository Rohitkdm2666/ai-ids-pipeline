<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body class="bg-dark text-light">
    <nav class="navbar navbar-expand-lg navbar-dark bg-black border-bottom border-secondary">
      <div class="container-fluid">
        <a class="navbar-brand fw-bold" href="/">AI IDS Platform</a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarNav"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <li class="nav-item">
              <a class="nav-link" href="/">Dashboard</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/live-traffic">Live Traffic</a>
            </li>
            <li class="nav-item">
              <a class="nav-link active" href="/attack-logs">Attack Logs</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/blocked-ips">Blocked IPs</a>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <main class="container py-4">
      <h1 class="mb-4">Attack Logs</h1>

      <div class="card bg-secondary text-light">
        <div class="card-body">
          <div class="d-flex justify-content-between align-items-center mb-3">
            <h5 class="card-title mb-0">Detected Attacks</h5>
            <span class="text-muted small">Showing latest <%= (recentAttacks || []).length %> entries</span>
          </div>
          <div class="table-responsive" style="max-height: 480px;">
            <table class="table table-sm table-dark table-striped align-middle">
              <thead>
                <tr>
                  <th>Time</th>
                  <th>Source IP</th>
                  <th>Dest IP</th>
                  <th>Dest Port</th>
                  <th>Severity</th>
                  <th>Probability</th>
                  <th>Label</th>
                </tr>
              </thead>
              <tbody>
                <% if (recentAttacks && recentAttacks.length) { %>
                  <% recentAttacks.forEach(function (a) { %>
                    <tr>
                      <td><%= a.detected_at && new Date(a.detected_at).toLocaleString() %></td>
                      <td><%= a.src_ip %></td>
                      <td><%= a.dest_ip %></td>
                      <td><%= a.dest_port %></td>
                      <td class="text-capitalize"><%= a.severity %></td>
                      <td><%= (a.probability != null) ? a.probability.toFixed(3) : '-' %></td>
                      <td><%= a.label %></td>
                    </tr>
                  <% }) %>
                <% } else { %>
                  <tr>
                    <td colspan="7" class="text-center text-muted">No attack logs yet.</td>
                  </tr>
                <% } %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
  </html>

